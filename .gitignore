# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Trained / exported model artifacts — regenerate with scripts/ (train -> export).
# These are produced locally and embedded at build time; they are not committed.
model/*.pt
model/*.onnx
model/labels.json
model/preprocess.json
model/calib/

# Logs / OS cruft
*.log
.DS_Store
